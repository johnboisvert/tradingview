[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["cd frontend && npm install && npm run build"]

[start]
cmd = "node_modules/.bin/serve frontend/dist --single --listen tcp://0.0.0.0:${PORT:-3000}"