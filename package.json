{
  "name": "cryptoia-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && node_modules/.bin/serve dist --single --listen $PORT"
  },
  "engines": {
    "node": ">=18"
  }
}