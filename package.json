{
  "name": "cryptoia-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "node_modules/.bin/serve frontend/dist --single --listen tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}